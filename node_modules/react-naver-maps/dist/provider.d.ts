import { ReactNode } from 'react';
import { ClientOptions } from './types/client.js';

declare type Props = ClientOptions & {
    children?: ReactNode;
};
declare function NavermapsProvider({ children, ...clientOptions }: Props): JSX.Element;

export { NavermapsProvider, Props };
