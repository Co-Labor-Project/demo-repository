declare type FilterFlags<Base, Condition> = {
    [Key in keyof Base]: Key extends Condition ? Key : never;
};
declare type AllowedKey<Base, Condition> = FilterFlags<Base, Condition>[keyof Base];

export { AllowedKey };
